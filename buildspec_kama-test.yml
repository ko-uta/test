version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 14
    commands:
      - n 16
  pre_build:
    commands:
      - node -v
  build:
    commands:
      - echo "test build"
      - ls -l
